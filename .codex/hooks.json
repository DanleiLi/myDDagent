{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq 'if (.tool_input.file_path // \"\") | contains(\".codex/schema\") or contains(\".codex\\\\schema\") then {continue: false, stopReason: \"Cannot write to .codex/schema folder - this folder is protected per AGETNS.md\"} else {continue: true} end'"
          }
        ]
      }
    ]
  }
}
