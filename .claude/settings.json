{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq 'if (.tool_input.file_path // \"\") | contains(\".claude/schema\") or contains(\".claude\\\\schema\") then {continue: false, stopReason: \"Cannot write to .claude/schema folder - this folder is protected per CLAUDE.md\"} else {continue: true} end'"
          }
        ]
      }
    ]
  }
}
